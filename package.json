{
  "name": "mtss",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env server/local.cjs",
    "dev": "npm start",
    "build": "node scripts/build.cjs",
    "test": "node --test test.cjs tests/*.test.cjs"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@libsql/client": "0.18.0",
    "pdfkit": "0.20.2",
    "pdfmake": "0.3.11"
  }
}
