# Vercel requires a persistent remote database. Local use falls back to SQLite.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Used only when creating the initial editor account in an empty database.
EDITOR_PASSWORD=123456
